# 🔐 Secrets Configuration Guide

## Обзор

Этот каталог содержит шаблоны и инструкции для настройки секретов проекта.

---

## Список необходимых секретов

### 1. BREVO_API_KEY (Обязательно)
**Назначение:** Отправка транзакционных email (регистрация, сброс пароля)

**Где получить:**
1. Зарегистрироваться на https://brevo.com
2. Перейти в Settings → SMTP & API → API Keys
3. Создать новый API ключ
4. Подтвердить домен отправителя в Settings → Senders & IP

**Как установить:**
```bash
supabase secrets set BREVO_API_KEY="xkeysib-your-key-here"
```

---

### 2. VAPID_PUBLIC_KEY и VAPID_PRIVATE_KEY (Обязательно для push-уведомлений)
**Назначение:** Web Push уведомления

**Как сгенерировать:**
```bash
# Установить web-push
npm install -g web-push

# Сгенерировать ключи
web-push generate-vapid-keys

# Вывод:
# Public Key: BN...
# Private Key: ...
```

**Как установить:**
```bash
supabase secrets set VAPID_PUBLIC_KEY="BN..."
supabase secrets set VAPID_PRIVATE_KEY="..."
```

---

### 3. WEBHOOK_SECRET (Обязательно)
**Назначение:** Защита внутренних webhooks (notify-new-place-webhook)

**Как сгенерировать:**
```bash
# Сгенерировать случайный секрет
openssl rand -hex 32
```

**Как установить:**
```bash
supabase secrets set WEBHOOK_SECRET="generated-secret-here"
```

---

### 4. LOVABLE_API_KEY (Опционально)
**Назначение:** AI переводы через Lovable AI

**Где получить:**
- Из Lovable Dashboard (если используете Lovable Cloud)
- Этот секрет опционален, если не используете функцию auto-translate

**Как установить:**
```bash
supabase secrets set LOVABLE_API_KEY="your-lovable-key"
```

---

## Автоматически настраиваемые переменные

Эти переменные устанавливаются Supabase автоматически:

- `SUPABASE_URL` - URL вашего проекта
- `SUPABASE_ANON_KEY` - Публичный ключ (anon)
- `SUPABASE_SERVICE_ROLE_KEY` - Секретный ключ с полными правами
- `SUPABASE_DB_URL` - URL подключения к БД
- `SUPABASE_PUBLISHABLE_KEY` - Альтернативное название для ANON_KEY

Эти переменные НЕ нужно устанавливать вручную!

---

## Установка всех секретов

### Метод 1: Через CLI (рекомендуется)

```bash
# Установить Brevo API Key
supabase secrets set BREVO_API_KEY="xkeysib-..."

# Установить VAPID ключи
supabase secrets set VAPID_PUBLIC_KEY="BN..."
supabase secrets set VAPID_PRIVATE_KEY="..."

# Установить Webhook Secret
supabase secrets set WEBHOOK_SECRET="$(openssl rand -hex 32)"

# (Опционально) Установить Lovable AI Key
supabase secrets set LOVABLE_API_KEY="..."
```

### Метод 2: Через Dashboard

1. Открыть Supabase Dashboard
2. Перейти в Project Settings → Edge Functions → Secrets
3. Нажать "Add secret"
4. Ввести имя и значение секрета
5. Нажать "Save"

---

## Проверка установленных секретов

```bash
# Список всех секретов (без значений)
supabase secrets list

# Вывод должен показать:
# BREVO_API_KEY
# VAPID_PUBLIC_KEY
# VAPID_PRIVATE_KEY
# WEBHOOK_SECRET
# LOVABLE_API_KEY (если установлен)
```

---

## Обновление секретов

```bash
# Обновить существующий секрет
supabase secrets set BREVO_API_KEY="new-key-value"
```

---

## Удаление секретов

```bash
# Удалить секрет
supabase secrets unset SECRET_NAME
```

---

## Использование секретов в Edge Functions

```typescript
// В любой edge функции:
const brevoApiKey = Deno.env.get('BREVO_API_KEY');
const vapidPublic = Deno.env.get('VAPID_PUBLIC_KEY');
const vapidPrivate = Deno.env.get('VAPID_PRIVATE_KEY');
const webhookSecret = Deno.env.get('WEBHOOK_SECRET');
```

---

## Безопасность

⚠️ **КРИТИЧЕСКИ ВАЖНО:**
- НИКОГДА не коммитьте реальные секреты в Git
- НЕ храните секреты в коде
- НЕ передавайте секреты на фронтенд
- Регулярно ротируйте секреты
- Используйте разные секреты для dev/staging/production

---

## Troubleshooting

### Проблема: Edge функция не видит секрет
**Решение:**
1. Проверить что секрет установлен: `supabase secrets list`
2. Пере-деплоить функцию: `supabase functions deploy function-name`
3. Проверить логи: `supabase functions logs function-name`

### Проблема: Email не отправляются
**Решение:**
1. Проверить BREVO_API_KEY установлен правильно
2. Проверить что домен подтверждён в Brevo
3. Проверить лимиты аккаунта Brevo
4. Проверить логи send-custom-email функции

### Проблема: Push уведомления не работают
**Решение:**
1. Проверить VAPID ключи установлены
2. Проверить что ключи сгенерированы правильно (web-push)
3. Проверить что браузер поддерживает Web Push
4. Проверить что пользователь дал разрешение на уведомления

---

## Дополнительные ресурсы

- [Supabase Secrets Documentation](https://supabase.com/docs/guides/cli/managing-config)
- [Brevo API Documentation](https://developers.brevo.com/)
- [Web Push Protocol](https://web.dev/push-notifications-overview/)
- [VAPID Keys Specification](https://tools.ietf.org/html/rfc8292)